# .env

# --- 시리얼 포트 설정 ---
# 라즈베리파이에 연결된 LoRa 모듈의 시리얼 포트와 통신 속도를 설정합니다.
#
# 라즈베리파이 내장 UART를 사용하는 경우:
#   - /dev/ttyS0 또는 /dev/serial0
# USB to Serial 어댑터를 사용하는 경우:
#   - /dev/ttyUSB0, /dev/ttyUSB1 등
#
# `ls /dev/tty*` 명령어로 사용 가능한 포트 목록을 확인할 수 있습니다.
SERIAL_PORT=/dev/ttyS0

# LoRa 모듈에 설정된 통신 속도와 동일해야 합니다. (일반적으로 9600 또는 115200)
BAUD_RATE=9600


# --- 데이터베이스 설정 ---
# 데이터베이스 파일의 위치를 지정합니다.
DATABASE_URL=sqlite:///./lora_data.db
